# Frontend environment variables
# If set, frontend will call backend directly; otherwise Next.js rewrites can proxy under /api
# Example: https://your-backend.up.railway.app
NEXT_PUBLIC_API_BASE_URL=
# Alternative variable used by next.config.mjs for rewrites if NEXT_PUBLIC_API_BASE_URL is empty
API_URL=
